{
  "name": "gridoffortune",
  "version": "2.9.0",
  "private": true,
  "description": "A drop-and-merge puzzle. Static web app, wrapped for iOS with Capacitor.",
  "scripts": {
    "serve": "python3 .claude/serve.py 4173",
    "build": "rm -rf www && mkdir -p www && cp index.html www/ && cp -R css js www/",
    "icons": "node tools/icon.js",
    "ios": "npm run sync && LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 DEVELOPER_DIR=\"${DEVELOPER_DIR:-/Applications/Xcode.app/Contents/Developer}\" npx cap open ios",
    "sync": "npm run build && LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 DEVELOPER_DIR=\"${DEVELOPER_DIR:-/Applications/Xcode.app/Contents/Developer}\" npx cap sync ios",
    "art": "node tools/art.js",
    "art:watch": "node tools/watch-art.js"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.8"
  },
  "dependencies": {
    "@capacitor/core": "^7.6.8",
    "@capacitor/ios": "^7.6.8"
  }
}
